export module funcs; // The name of the module

// export keyword lets us to use the declared things
export const double PI{3.14};
export double CircleArea(double radius);
export double CircleCircumference(double radius);